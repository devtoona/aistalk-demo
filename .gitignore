# OS
.DS_Store

# Secrets / credentials
key.json
**/secret/**
**/*.key
**/*.pem
.env
.env.*
!.env.example
!**/.env.example

# Local-only (not part of OSS demo)
coeiroink/
uploads/

# Production-only (kept locally, not in OSS repo)
cloudbuild-db-migrate.yaml
cloudbuild-admin-frontend.yaml
infrastructure/